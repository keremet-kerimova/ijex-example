import { useState } from "react";
import classes from "./Author.module.css";

function Author(posts) {
   
    return (
        <div className={classes.Author}>{user.Id}</div>
    );
  
}
export default Author;

